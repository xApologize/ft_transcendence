import {AppBar, Container} from "@mui/material";
function header() {
    return (
        <>
            <AppBar position="static">
                <Container max>

                </Container>
            </AppBar>
        </>
    )
}

export default header;